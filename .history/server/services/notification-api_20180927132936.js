function Get (id) {
  console.log('calling API PUT to update existing notification with id ' + id)
}

function Put (id) {
  console.log('calling API PUT to update existing notification with id ' + id)
}

module.exports.apiCreate = Get
module.exports.apiEdit = Put
